---
title: "{{ title }}"
slug: "{{ slug }}"
date: {{ date }}
series: "{{ series }}"
chapter: "{{ chapter }}"
article_variant: "spoiler"
{% if tags %}
tags:
{% for tag in tags %}
  - "{{ tag }}"
{% endfor %}
{% else %}
tags: []
{% endif %}
draft: {{ draft | lower }}
description: "{{ description }}"
affiliate_ids:
  amazon: "{{ affiliate_ids.amazon }}"
  rakuten: "{{ affiliate_ids.rakuten }}"
{% if affiliate_ids.others %}
  others:
{% for other in affiliate_ids.others %}
    - name: "{{ other.name }}"
      url: "{{ other.url }}"
{% endfor %}
{% else %}
  others: []
{% endif %}
disclaimer: "{{ disclaimer }}"
{% if images %}
images:
{% for image in images %}
  - "{{ image }}"
{% endfor %}
{% else %}
images: []
{% endif %}
---
{{ intro }}

> **{{ spoiler_notice | default("本記事は最新話までのネタバレを含みます。") }}**

{% if hero_image %}
![{{ hero_image.alt }}]({{ hero_image.url }})
<small>Photo by <a href="{{ hero_image.photographer_url }}" target="_blank" rel="noopener">{{ hero_image.photographer }}</a> on <a href="{{ hero_image.pexels_url }}" target="_blank" rel="noopener">Pexels</a></small>
{% endif %}

## 結論：第{{ chapter_label }}のポイント3つ（ネタバレあり）
{% for point in summary_points %}
- {{ point }}
{% endfor %}

{{ "{{< spoiler >}}" }}
## 第{{ chapter_label }}のあらすじ要約（3分でわかる）
{{ spoiler.synopsis }}

## 詳細考察：キャラ・物語・テーマを深掘り
### キャラの変化・関係性
{% for item in spoiler.foreshadowings %}
- {{ item }}
{% endfor %}

### 世界観・設定の掘り下げ
- （世界観や設定に関する気づきをここに追記）

### 気になるセリフ・描写
- （印象的なセリフ・演出に関する考察をここに追記）

## 伏線・回収ポイントの整理
{% for item in spoiler.foreshadowings %}
- {{ item }}
{% endfor %}

## 今後の展開予想（原作既読の範囲内）
{% for item in spoiler.predictions %}
- {{ item }}
{% endfor %}

## 関連巻・グッズへのリンク
- 第{{ chapter_label }}収録巻：{{ affiliate_ids.amazon or "（Amazonリンク未設定）" }}
- 電子書籍：{{ series }}（Kindle / 楽天Kobo 等）
{{ "{{< /spoiler >}}" }}

## 関連記事・用語集
- 関連話へのリンク：{{ official_link.url if official_link else "" }}
- 用語集へのリンク：{{ .Site.BaseURL }}posts/{{ series_slug }}/glossary/
